"use client";
import Link from "next/link"; import { Menu, X } from "lucide-react"; import { useState } from "react"; import { OrderButton } from "./OrderButton"; import { CartButton } from "./cart/CartButton";
export function Navbar(){const[open,setOpen]=useState(false);const links=[['Menu','/menu'],['Contact','/contact']];return <header className="fixed inset-x-0 top-0 z-50 border-b border-earth/10 bg-cream/90 backdrop-blur-md"><nav className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 lg:px-10"><Link href="/" className="focus-ring font-display text-lg tracking-[.08em] text-forest sm:text-xl">DE VIHELHANA <span className="text-gold">BISTROT</span></Link><div className="hidden items-center gap-5 md:flex">{links.map(([name,href])=><Link className="focus-ring text-sm text-forest/80 transition hover:text-earth" href={href} key={href}>{name}</Link>)}<CartButton/><OrderButton/></div><div className="flex items-center gap-2 md:hidden"><CartButton/><button aria-label="Toggle navigation" onClick={()=>setOpen(!open)} className="focus-ring p-2">{open?<X/>:<Menu/>}</button></div></nav>{open&&<div className="border-t border-earth/10 bg-cream px-5 py-5 md:hidden">{links.map(([name,href])=><Link onClick={()=>setOpen(false)} className="block py-3 text-lg font-display" href={href} key={href}>{name}</Link>)}<div className="pt-3"><OrderButton/></div></div>}</header>}
