"use client";

import Link from "next/link";
import { useState } from "react";
import { ArrowLeft, LoaderCircle, MapPin } from "lucide-react";
import { DeliveryNoticeModal } from "@/components/DeliveryNoticeModal";
import { CartSummary } from "./CartSummary";
import { formatPrice, useCart } from "./CartProvider";

type FormData = { name: string; phone: string; email: string; orderType: "" | "pickup"; notes: string };
const empty: FormData = { name: "", phone: "", email: "", orderType: "", notes: "" };

export function CheckoutScreen() {
  const { items, subtotal, hotelDelivery } = useCart();
  const [data, setData] = useState(empty);
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [deliveryNoticeOpen, setDeliveryNoticeOpen] = useState(false);
  const [paying, setPaying] = useState(false);
  const set = (key: keyof FormData, value: string) => setData(current => ({ ...current, [key]: value }));
  const field = (key: "name" | "phone" | "email", label: string, type = "text") => <label className="block"><span className="text-xs font-semibold uppercase tracking-[.15em] text-earth">{label}</span><input value={data[key]} onChange={event => set(key, event.target.value)} type={type} className="focus-ring mt-2 w-full border-b border-earth/30 bg-transparent py-3 text-sm" aria-invalid={!!errors[key]} />{errors[key] && <span className="mt-1 block text-xs text-red-700">{errors[key]}</span>}</label>;
  async function submit(event: React.FormEvent) {
    event.preventDefault(); const next: Record<string, string> = {};
    if (!hotelDelivery) { if (!data.name.trim()) next.name = "Please enter your full name."; if (!data.phone.trim()) next.phone = "Please enter your phone number."; if (!/^\S+@\S+\.\S+$/.test(data.email)) next.email = "Please enter a valid email."; if (!data.orderType) next.orderType = "Choose pickup or hotel delivery."; }
    if (!items.length) next.order = "Your cart is empty."; setErrors(next); if (Object.keys(next).length) return;
    const delivery = hotelDelivery ? hotelDelivery.location === "room" ? { type: "hotel-room", guestName: hotelDelivery.guestName, phone: hotelDelivery.phone, floor: hotelDelivery.floor, room: hotelDelivery.room, instructions: hotelDelivery.instructions } : { type: "pool-area", guestName: hotelDelivery.guestName, phone: hotelDelivery.phone, locationDetails: hotelDelivery.poolDetails, instructions: hotelDelivery.instructions } : { type: "pickup", name: data.name, phone: data.phone, email: data.email, notes: data.notes };
    setPaying(true); try { const response = await fetch("/api/checkout", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ items: items.map(item => ({ id: item.id, quantity: item.quantity })), delivery }) }); const body = await response.text(); let payload: { url?: string; error?: string } = {}; try { payload = body ? JSON.parse(body) : {}; } catch {} if (!response.ok || !payload.url) throw new Error(payload.error || "Stripe is not configured. Add your Stripe Test Mode keys to .env.local, then restart the server."); window.location.assign(payload.url); } catch (error) { setErrors({ order: error instanceof Error ? error.message : "Unable to start payment." }); setPaying(false); }
  }
  return <main className="min-h-screen px-5 pb-20 pt-32 lg:px-10"><div className="mx-auto max-w-6xl"><p className="eyebrow">Checkout</p><h1 className="mt-3 font-display text-4xl sm:text-5xl">Almost <i className="text-earth">there.</i></h1><div className="mt-10 grid gap-10 lg:grid-cols-[1fr_360px]"><form onSubmit={submit} noValidate className="rounded-[2rem] bg-beige/45 p-6 sm:p-10">{hotelDelivery ? <div><p className="eyebrow">Hotel delivery</p><h2 className="mt-3 font-display text-2xl">Delivery details</h2><div className="mt-6 rounded-2xl border border-earth/15 bg-cream/50 p-5 text-sm leading-7"><p><strong>Guest:</strong> {hotelDelivery.guestName}</p><p><strong>Phone:</strong> {hotelDelivery.phone}</p><p className="mt-3 flex items-center gap-2 font-semibold"><MapPin size={16} className="text-earth"/>{hotelDelivery.location === "room" ? `Hotel Room ${hotelDelivery.room} · ${hotelDelivery.floor}` : "Pool Area"}</p>{hotelDelivery.location === "pool" && hotelDelivery.poolDetails && <p><strong>Location details:</strong> {hotelDelivery.poolDetails}</p>}{hotelDelivery.instructions && <p><strong>Special instructions:</strong> {hotelDelivery.instructions}</p>}</div><Link href="/hotel-delivery" className="focus-ring mt-5 inline-flex text-xs font-semibold tracking-[.12em] text-earth hover:text-forest">EDIT HOTEL DELIVERY DETAILS</Link></div> : <><h2 className="font-display text-2xl">Customer details</h2><div className="mt-7 grid gap-6 sm:grid-cols-2">{field("name", "Full name")}{field("phone", "Phone number")}{field("email", "Email", "email")}</div><fieldset className="mt-10"><legend className="text-xs font-semibold uppercase tracking-[.15em] text-earth">Order type</legend><div className="mt-3 flex flex-wrap gap-3"><button type="button" onClick={() => set("orderType", "pickup")} className={`focus-ring rounded-full border px-5 py-2 text-xs font-semibold tracking-[.13em] ${data.orderType === "pickup" ? "border-forest bg-forest text-cream" : "border-earth/25 text-forest"}`}>PICKUP</button><button type="button" onClick={() => setDeliveryNoticeOpen(true)} className="focus-ring rounded-full border border-earth/25 px-5 py-2 text-xs font-semibold tracking-[.13em] text-forest">DELIVERY</button></div>{errors.orderType && <span className="mt-2 block text-xs text-red-700">{errors.orderType}</span>}</fieldset><label className="mt-8 block"><span className="text-xs font-semibold uppercase tracking-[.15em] text-earth">Order notes (optional)</span><textarea value={data.notes} onChange={event => set("notes", event.target.value)} rows={3} className="focus-ring mt-2 w-full resize-none border-b border-earth/30 bg-transparent py-3 text-sm" /></label></>}{errors.order && <span className="mt-5 block text-xs text-red-700">{errors.order}</span>}<button disabled={paying} className="focus-ring mt-8 inline-flex items-center gap-2 rounded-full bg-forest px-6 py-3 text-xs font-semibold tracking-[.15em] text-cream transition hover:bg-earth disabled:opacity-60">{paying && <LoaderCircle className="animate-spin" size={15}/>}PAY SECURELY</button></form><aside className="h-fit border border-earth/15 p-7"><p className="eyebrow">Order summary</p><div className="mt-4 space-y-4">{items.map(item => <div className="flex justify-between gap-3 text-sm" key={item.id}><span>{item.quantity} × {item.name}</span><span className="shrink-0 text-earth">{formatPrice(item.unitPrice * item.quantity)}</span></div>)}</div><div className="mt-6"><CartSummary/></div><p className="mt-4 text-xs text-forest/60">Subtotal {formatPrice(subtotal)} · Total {formatPrice(subtotal)}</p><Link href="/cart" className="focus-ring mt-6 inline-flex items-center gap-2 text-xs font-semibold tracking-[.12em] text-earth hover:text-forest"><ArrowLeft size={14}/>EDIT CART</Link></aside></div></div>{deliveryNoticeOpen && <DeliveryNoticeModal onClose={() => setDeliveryNoticeOpen(false)} />}</main>;
}
