"use client";
import Link from "next/link";
import { ArrowLeft, ArrowRight } from "lucide-react";
import { CartSummary } from "./CartSummary";
import { QuantityControl } from "./QuantityControl";
import { formatPrice, useCart } from "./CartProvider";
export function CartScreen(){const{items}=useCart();return <main className="min-h-screen px-5 pb-20 pt-32 lg:px-10"><div className="mx-auto max-w-5xl"><p className="eyebrow">Your selection</p><h1 className="mt-3 font-display text-4xl sm:text-5xl">Your <i className="text-earth">cart.</i></h1>{items.length===0?<div className="mt-10 border-y border-earth/15 py-14"><p className="font-display text-2xl">Your cart is waiting for something delicious.</p><Link href="/menu" className="focus-ring mt-6 inline-flex items-center gap-2 rounded-full bg-forest px-5 py-3 text-xs font-semibold tracking-[.15em] text-cream transition hover:bg-earth">EXPLORE MENU <ArrowRight size={15}/></Link></div>:<div className="mt-10 grid gap-10 lg:grid-cols-[1fr_320px]"><div>{items.map(item=><article key={item.id} className="border-b border-earth/15 py-6"><div className="flex items-start justify-between gap-5"><div><p className="eyebrow">{item.category}</p><h2 className="mt-2 font-display text-xl">{item.name}</h2><p className="mt-2 text-sm text-forest/65">{formatPrice(item.unitPrice)} each</p></div><p className="font-display text-lg text-earth">{formatPrice(item.unitPrice*item.quantity)}</p></div><div className="mt-4"><QuantityControl item={item}/></div></article>)}</div><aside className="h-fit rounded-[2rem] bg-beige/45 p-7"><p className="eyebrow">Order summary</p><CartSummary/><p className="mt-4 text-xs leading-5 text-forest/60">Taxes and any applicable delivery charge are confirmed at checkout.</p><Link href="/checkout" className="focus-ring mt-6 flex items-center justify-center gap-2 rounded-full bg-forest px-5 py-3 text-xs font-semibold tracking-[.15em] text-cream transition hover:bg-earth">PROCEED TO CHECKOUT <ArrowRight size={15}/></Link><Link href="/menu" className="focus-ring mt-4 flex items-center justify-center gap-2 text-xs font-semibold tracking-[.12em] text-earth hover:text-forest"><ArrowLeft size={14}/>CONTINUE SHOPPING</Link></aside></div>}</div></main>}
