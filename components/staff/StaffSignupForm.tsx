"use client";
import { useState } from "react";
import Link from "next/link";
export function StaffSignupForm() { const [data, setData] = useState({ fullName: "", email: "", password: "", confirmPassword: "" }); const [error, setError] = useState(""); const [success, setSuccess] = useState(false); const [loading, setLoading] = useState(false); const set = (key: keyof typeof data, value: string) => setData((current) => ({ ...current, [key]: value })); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(""); try { const response = await fetch("/api/staff/auth/signup", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); const payload = await response.json().catch(() => ({})); if (!response.ok) throw new Error(payload.error || "Unable to register."); setSuccess(true); } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to register."); } finally { setLoading(false); } } if (success) return <div className="mt-8 rounded-2xl bg-forest p-6 text-cream"><p className="font-display text-2xl">Registration successful.</p><p className="mt-3 text-sm leading-7 text-cream/75">Your account is waiting for administrator approval. You will be able to access the Staff Dashboard after your account has been approved.</p><Link href="/staff/login" className="focus-ring mt-5 inline-flex rounded-full bg-cream px-5 py-3 text-xs font-semibold tracking-[.14em] text-forest">GO TO STAFF LOGIN</Link></div>; return <form onSubmit={submit} className="mt-8 space-y-6"><label className="block"><span className="text-xs font-semibold uppercase tracking-[.15em] text-earth">Full name</span><input autoComplete="name" value={data.fullName} onChange={(event) => set("fullName", event.target.value)} className="focus-ring mt-2 w-full border-b border-earth/30 bg-transparent py-3 text-sm" required /></label><label className="block"><span className="text-xs font-semibold uppercase tracking-[.15em] text-earth">Email</span><input autoComplete="email" type="email" value={data.email} onChange={(event) => set("email", event.target.value)} className="focus-ring mt-2 w-full border-b border-earth/30 bg-transparent py-3 text-sm" required /></label><label className="block"><span className="text-xs font-semibold uppercase tracking-[.15em] text-earth">Password</span><input autoComplete="new-password" type="password" value={data.password} onChange={(event) => set("password", event.target.value)} className="focus-ring mt-2 w-full border-b border-earth/30 bg-transparent py-3 text-sm" required /><span className="mt-2 block text-xs text-forest/60">At least 10 characters with uppercase, lowercase, and a number.</span></label><label className="block"><span className="text-xs font-semibold uppercase tracking-[.15em] text-earth">Confirm password</span><input autoComplete="new-password" type="password" value={data.confirmPassword} onChange={(event) => set("confirmPassword", event.target.value)} className="focus-ring mt-2 w-full border-b border-earth/30 bg-transparent py-3 text-sm" required /></label>{error && <p role="alert" className="text-sm text-red-700">{error}</p>}<button disabled={loading} className="focus-ring min-h-12 rounded-full bg-forest px-6 py-3 text-xs font-semibold tracking-[.15em] text-cream transition hover:bg-earth disabled:opacity-60">{loading ? "REGISTERING…" : "CREATE STAFF ACCOUNT"}</button></form>; }
